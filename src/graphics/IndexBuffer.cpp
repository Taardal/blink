#include "IndexBuffer.h"

namespace Blink {

    bool IndexBuffer::initialize() {
        return true;
    }

    void IndexBuffer::terminate() const {
    }

    void IndexBuffer::bind() const {
    }

    void IndexBuffer::unbind() const {
    }

    void IndexBuffer::setData(const IndexBufferData& data) const {
    }

    void IndexBuffer::setSubData(const IndexBufferSubData& data) const {
    }
}