#include "Shader.h"

namespace Blink {
    bool Shader::initialize(const std::string& vertexShaderSrc, const std::string& fragmentShaderSrc) {
        return true;
    }

    void Shader::terminate() const {
    }
}
